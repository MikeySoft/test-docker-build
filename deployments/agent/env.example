# Flotilla Agent sample environment file

# --- Logging ---
LOG_LEVEL=info
LOG_FORMAT=json

# --- Server connection ---
# Address/port of the management server (Hub)
SERVER_ADDRESS=localhost
SERVER_PORT=8080
# Use TLS (wss) when connecting to the server
SERVER_USE_TLS=false
# Agent API key configured on the server (required when not localhost)
API_KEY=

# --- Agent identity ---
# Optional fixed ID; if empty, generated and persisted in volume
AGENT_ID=
# Friendly name; defaults to container hostname if empty
AGENT_NAME=

# --- Docker ---
# Socket path inside the container
DOCKER_SOCKET=/var/run/docker.sock

# --- Connection/heartbeat ---
AGENT_HEARTBEAT_INTERVAL=30s
AGENT_RECONNECT_INTERVAL=5s
AGENT_MAX_RECONNECT_ATTEMPTS=10

# --- Metrics ---
METRICS_ENABLED=true
METRICS_COLLECTION_INTERVAL=30s
# Host metrics off by default. Set to true or "auto" to enable autodetect
METRICS_COLLECT_HOST_STATS=false
# Network metrics on by default
METRICS_COLLECT_NETWORK=true

# Optional disk I/O fallback (normally auto-detected)
# When true, forces cgroup-based IO metrics if Docker blkio is missing
METRICS_COLLECT_DISK_IO_FALLBACK=false

# Host paths for cgroup/proc when running in a container
# Usually mounted via: -v /proc:/host/proc:ro -v /sys/fs/cgroup:/host/sys/fs/cgroup:ro
HOST_PROC_ROOT=/host/proc
HOST_CGROUP_ROOT=/host/sys/fs/cgroup
# Optional host root path used for disk usage probing and UI
HOST_ROOT_PATH=/

